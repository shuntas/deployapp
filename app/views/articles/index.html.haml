.title 
  Deploy App

.form
  = form_for @article do |f|
    = f.text_field :text
    = f.file_field :image
    = f.submit

- if @articles
  %ul
    - @articles.each do |article|
      .box
        %li
          = image_tag article.image
        %li
          = article.text